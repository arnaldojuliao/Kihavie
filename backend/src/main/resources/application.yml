spring:
  application:
    name: backend

  datasource:
    url: jdbc:postgresql://localhost:5432/kihavie_db
    username: arnaldojuliaoj2
    password: 849352
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update   # alterado para update para migrar as colunas de BIGINT para VARCHAR(50) após a mudança dos IDs
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

server:
  port: ${PORT:8080}

jwt:
  secret: chaveSuperSecretaComPeloMenos32Caracteres!
  expiration: 86400000

file:
  upload-dir: ./uploads