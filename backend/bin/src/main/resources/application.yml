spring:
  application:
    name: backend

  datasource:
    url: jdbc:postgresql://localhost:5432/kihavie_db
    username: arnaldojuliaoj2
    password: 849352
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate   # as tabelas já foram criadas; validate evita tentativas de alteração sem permissão
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

server:
  port: 5000

jwt:
  secret: chaveSuperSecretaComPeloMenos32Caracteres!
  expiration: 86400000

file:
  upload-dir: ./uploads